---
- name: Install standard development tools (Ubuntu) # No packages left after removing overlap
  ansible.builtin.apt:
    name: []
    state: present
  become: true

- name: Install modern CLI tools (Ubuntu) # No packages left after removing overlap
  ansible.builtin.apt:
    name: []
    state: present
  become: true
  # ignore_errors: true # Removed
# eza installation for Ubuntu is now handled in special_installs.yml
